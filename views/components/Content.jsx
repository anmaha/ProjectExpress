import React from 'react'

//const { contentStyle } = require('../../styles/contentStyle')

const Content = (props) => {
    return (
        <div>
            {props.children}
        </div>
    )
}

export default Content